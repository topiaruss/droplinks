module.exports = async function globalSetup() {
  console.log('Setting up global test environment...');
  // Global setup for Playwright tests
  // You can set up test databases, start servers, etc. here
};